import React from 'react';

const EventItem = (props) => {
    return (
        <div className={event_item}>
            <h2>props.title</h2>
            
        </div>
    );
};

export default EventItem;